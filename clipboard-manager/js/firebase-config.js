/* ============================================================
 * Firebase 設定（クラウド同期）
 * ------------------------------------------------------------
 * ここが空（プレースホルダのまま）の場合は、クラウド同期は無効になり、
 * これまで通りブラウザ内（localStorage）だけで動作します。
 *
 * ▼ 同期を有効にする手順（無料）
 *   1. https://console.firebase.google.com/ でプロジェクトを作成
 *   2. 「Firestore Database」を作成（本番モード or テストモード）
 *   3. プロジェクト設定 →「ウェブアプリ」を追加し、firebaseConfig の値をコピー
 *   4. 下の値を貼り付けて保存（コミット）
 *
 * ※ ここに入る apiKey などは「公開してよい」クライアント用の識別子です
 *   （秘密鍵ではありません）。実際のアクセス制御は Firestore のセキュリティ
 *   ルールで行います。詳しくは README を参照。
 * ============================================================ */
window.COPYPAD_FIREBASE_CONFIG = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};

/* 全員で共有する「ワークスペース（共有ドキュメント）」のID。
 * 同じIDを見ている人どうしが、同じリストをリアルタイムで共有・共同編集します。
 * 別のチーム用に分けたい場合はこの文字列を変えてください。 */
window.COPYPAD_WORKSPACE_ID = "team-main";
